import { DirEstDirective } from './dir-est.directive';

describe('DirEstDirective', () => {
  it('should create an instance', () => {
    const directive = new DirEstDirective();
    expect(directive).toBeTruthy();
  });
});
