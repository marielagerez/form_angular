import { DirAtrDirective } from './dir-atr.directive';
import { ElementRef } from '@angular/core';

describe('DirAtrDirective', () => {
  it('should create an instance', () => {
    const directive = new DirAtrDirective();
    expect(directive).toBeTruthy();
  });
});
